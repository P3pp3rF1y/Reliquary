package reliquary.compat.waila;

public class WailaCompat {
	public void setup() {
/*  TODO implement waila compatibility
		if (phase == InitializationPhase.INIT)
			FMLInterModComms.sendMessage(getModId(), "register", "reliquary.compat.waila.WailaCallbackHandler.callbackRegister");
*/
	}
}
